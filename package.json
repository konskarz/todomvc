{
  "name": "todomvc",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sdb": "fastify start -w -l info -P synceddb-fastify-server.js"
  },
  "dependencies": {
    "@platformatic/sql-mapper": "^1.52.2",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "fastify-cli": "^6.3.0"
  }
}
